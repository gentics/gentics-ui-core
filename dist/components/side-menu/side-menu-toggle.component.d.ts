/**
 * An animated CSS hamburger menu icon, used by the SideMenu component to trigger the
 * "toggle" event. Only used internally by the SideMenu component.
 */
export declare class SideMenuToggleButton {
    active: boolean;
}
